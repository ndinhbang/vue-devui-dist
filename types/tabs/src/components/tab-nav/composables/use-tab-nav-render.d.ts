import { TabsProps } from '../../../tabs-types';
import { OffSetData, UseTabNavRender } from '../tab-nav-types';
export declare function useTabNavRender(props: TabsProps, data: OffSetData): UseTabNavRender;
