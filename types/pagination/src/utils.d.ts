/**
 * 功能函数
 */
export declare const handlePages: (cursor: number, maxItems: number, totalPages: number) => number[];
export declare function liteSelectOptions(total: number): Array<{
    name: string;
    value: number;
}>;
