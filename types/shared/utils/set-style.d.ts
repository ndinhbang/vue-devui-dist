import type { CSSProperties } from 'vue';
/**
 * 设置元素的样式，返回上一次的样式
 * @param element
 * @param style
 * @returns
 */
export declare function setStyle(element: HTMLElement, style: CSSProperties): CSSProperties;
